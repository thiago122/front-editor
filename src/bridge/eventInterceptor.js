/** @placeholder — implementação futura @see docs/bridge-architecture.md */
export function setupEvents(_config) {
  // TODO: listeners em capture phase para click, mouseover, mouseleave
  // Quando window.__editorActive === true → stopImmediatePropagation()
  // e notifica o editor via postMessage
}
