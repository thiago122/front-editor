/** @placeholder — implementação futura @see docs/bridge-architecture.md */
export function setupMutation(_config) {
  // TODO: MutationObserver que re-injeta data-node-id em elementos movidos pelo CMS
  // Usar window.__editorMutating = true/false para evitar loop
}
